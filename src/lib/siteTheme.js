import { createTheme } from "@mui/material";

const siteTheme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#f28a00",
        },
        secondary: {
            main: "#0069f2",
        },
    },
});

export default siteTheme;
